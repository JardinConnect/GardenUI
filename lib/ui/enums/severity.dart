/// Severity enums for all components in the Garden UI library.
///
/// This file contains all severity variant enums used across different components
/// to maintain consistency and type safety.
library;

/// Severity variants for button component
enum ButtonSeverity { primary, danger }

/// Severity variants for menu item component
enum MenuItemSeverity { normal, danger }

/// Severity variants for menu icon component
enum MenuIconSeverity { normal, danger }

/// Severity variants for notification toast component
enum NotificationSeverity { alert, info, success, warning }

/// Severity variants for notification icon component
enum NotificationIconSeverity { alert, info, success, warning }
