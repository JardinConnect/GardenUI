/// Size enums for all components in the Garden UI library.
///
/// This file contains all size variant enums used across different components
/// to maintain consistency and type safety.
library;

/// Size variants for notification components
enum NotificationSize { sm, md, lg }

/// Size variants for alert indicator component
enum AlertIndicatorSize { sm, md, lg }

/// Size variants for level indicator component
enum LevelIndicatorSize { sm, md, lg }

/// Size variants for menu icon component
enum MenuIconSize { sm, md, lg }

/// Size variants for menu item component
enum MenuItemSize { sm, md, lg }

/// Size variants for menu component
enum MenuSize { sm, md, lg }

/// Size variants for hierarchical menu item component
enum HierarchicalMenuItemSize { sm, md, lg }

/// Size variants for hierarchical menu component
enum HierarchicalMenuSize { sm, md, lg }

/// Size variants for garden icon component
enum GardenIconSize { sm, md, lg }

/// Size variants for Tooltip component
enum TooltipSize { sm, md, lg }
